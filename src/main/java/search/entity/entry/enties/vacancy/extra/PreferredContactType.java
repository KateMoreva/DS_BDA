package search.entity.entry.enties.vacancy.extra;

import search.entity.entry.AbstractEntityEntry;

public final class PreferredContactType extends AbstractEntityEntry {
    public static final PreferredContactType NULL_PREFERRED_CONTACT_TYPE = new PreferredContactType();

    private PreferredContactType() {
    }
}
